import { Pressable, Text, type PressableProps, StyleSheet } from "react-native";
import { Colors } from "@/src/constants/Colors";
export function Button({ label, variant = "primary", ...props }: PressableProps & { label: string; variant?: "primary" | "secondary" | "ghost" }) { return <Pressable {...props} style={({ pressed }) => [styles.base, styles[variant], pressed && styles.pressed, props.style as object]}><Text style={[styles.label, variant === "primary" ? styles.primaryLabel : styles.secondaryLabel]}>{label}</Text></Pressable>; }
const styles = StyleSheet.create({ base: { minHeight: 52, borderRadius: 18, alignItems: "center", justifyContent: "center", paddingHorizontal: 20 }, primary: { backgroundColor: Colors.accent }, secondary: { backgroundColor: Colors.surfaceRaised, borderColor: Colors.border, borderWidth: 1 }, ghost: { backgroundColor: "transparent" }, pressed: { opacity: 0.78, transform: [{ scale: 0.98 }] }, label: { fontSize: 16, fontWeight: "800" }, primaryLabel: { color: Colors.background }, secondaryLabel: { color: Colors.foreground } });
