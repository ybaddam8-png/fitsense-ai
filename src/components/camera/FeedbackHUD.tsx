import { Text, View, StyleSheet } from "react-native";
import type { ExerciseAnalysis } from "@/src/types/pose";
import { Colors } from "@/src/constants/Colors";
import { phaseLabel } from "@/src/core/engine/StateMachine";
export function FeedbackHUD({ analysis, reps, elapsed }: { analysis: ExerciseAnalysis; reps: number; elapsed: string }) { const color = analysis.tone === "positive" ? Colors.accent : analysis.tone === "warning" ? Colors.orange : Colors.foreground; return <View style={styles.wrap}><View style={styles.top}><View><Text style={styles.kicker}>LIVE FEEDBACK</Text><Text style={[styles.feedback, { color }]}>{analysis.feedback}</Text></View><Text style={[styles.score, { color }]}>{Math.round(analysis.score)}%</Text></View><View style={styles.metrics}><View><Text style={styles.metricLabel}>REPS</Text><Text style={styles.metricValue}>{reps}</Text></View><View><Text style={styles.metricLabel}>TIME</Text><Text style={styles.metricValue}>{elapsed}</Text></View><View><Text style={styles.metricLabel}>PHASE</Text><Text style={styles.metricValue}>{phaseLabel(analysis.phase)}</Text></View></View></View>; }
const styles = StyleSheet.create({ wrap: { backgroundColor: "rgba(11,15,13,0.88)", borderRadius: 24, padding: 18, gap: 18 }, top: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, kicker: { color: Colors.muted, fontSize: 10, fontWeight: "800", letterSpacing: 1.5 }, feedback: { fontSize: 17, fontWeight: "800", maxWidth: 245, marginTop: 5 }, score: { fontSize: 30, fontWeight: "900" }, metrics: { flexDirection: "row", gap: 30 }, metricLabel: { color: Colors.muted, fontSize: 10, fontWeight: "800", letterSpacing: 1.2 }, metricValue: { color: Colors.foreground, fontSize: 18, fontWeight: "800", marginTop: 4 } });
